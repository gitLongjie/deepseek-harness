/**
 * Scheduled-work management plugin, node half. The empty apply keeps the
 * browser feature addressable from the Loader composition; the page lives
 * entirely in the browser entry.
 */

/** Host plugin body — scheduled-work management exists only in the browser entry. */
export function apply(): void {}
